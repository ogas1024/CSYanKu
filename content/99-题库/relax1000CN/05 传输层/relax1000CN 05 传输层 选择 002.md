---
source: relax1000CN
subject: CN
chapter: 05 传输层
qtype: 选择
answer: C
answer_status: 已录入
topic: []
tags: []
---

## 题干

9. 下列关于 UDP 协议的说法中，错误的是（ ）。

A. 接收方 UDP 发现收到的报文中目的端口不正确，会将该报文丢弃，并发送 ICMP 差错报文

B. 与 IP 数据报不同，UDP 的校验和会将首部和数据部分一起都校验

C. UDP 之间的通信需要在两个套接字之间建立连接

D. 伪首部仅用于计算 UDP 校验和，不向下传送也不向上递交

> [!note]- 原题截图
> ![[relax1000CN 05 传输层 选择 002.webp]]

> [!success]- 答案与解析
> **答案：** C
>
> **解析：** 虽然在 UDP 之间的通信要用到其端口号，但是 UDP 的通信是无连接的，而 TCP 之间的通信必须要在两个套接字之间建立连接，故选择 C。如果接收方 UDP 发现收到的报文中的目的端口号不正确（即不存在对应于该端口号的应用进程），就丢弃该报文，并发送 ICMP“端口不可达”差错报文给发送方，故 A 正确。在计算校验和时，要在 UDP 用户数据报之前增加 12 个字节的伪首部，伪首部仅用于计算校验和，会和原 UDP 数据报的首部和数据部分一起参与计算，故 B、D 正确。

## 关联

- 知识点：
- 题型：
