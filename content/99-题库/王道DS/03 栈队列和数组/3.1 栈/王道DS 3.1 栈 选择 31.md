---
source: 王道DS
subject: DS
chapter: 03 栈队列和数组
section: 3.1 栈
qtype: 选择
topic: []
answer: D
answer_status: 已录入
tags: []
---
## 题干

31. 【2022 统考真题】给定有限符号集 $S$，`in` 和 `out` 均为 $S$ 中所有元素的任意排列。

对于初始为空的栈 `ST`，下列叙述中，正确的是（ ）。

A. 若 `in` 是 `ST` 的入栈序列，则不能判断 `out` 是否为其可能的出栈序列

B. 若 `out` 是 `ST` 的出栈序列，则不能判断 `in` 是否为其可能的入栈序列

C. 若 `in` 是 `ST` 的入栈序列，`out` 是对应 `in` 的出栈序列，则 `in` 与 `out` 一定不同

D. 若 `in` 是 `ST` 的入栈序列，`out` 是对应 `in` 的出栈序列，则 `in` 与 `out` 可能互为倒序

> [!note]- 原题截图
> ![[王道DS 3.1 栈 选择 31.png]]

> [!success]- 答案与解析
> **答案：** D
>
> **解析：** 给定一个排列后可用栈操作模拟判断另一个排列是否合法，故 A、B 错。每个元素入栈后立即出栈可使 `in=out`；全部入栈后再出栈可使两者互为倒序，故 D 正确。

## 关联

- 知识点：
- 题型：
