---
source: 王道DS
subject: DS
chapter: 07 查找
section: 7.5 散列表
qtype: 综合
topic: []
answer_status: 已录入
tags: []
---
## 题干

2. 假定把关键字 $key$ 散列到有 $n$ 个表项（从 0 到 $n-1$ 编址）的散列表中。

对于下面的每个函数 $H(key)$（$key$ 为整数），这些函数能够当作散列函数吗？若能，它是一个好的散列函数吗？说明理由。

设函数 $random(n)$ 返回一个 0 到 $n-1$ 之间的随机整数（包括 0 与 $n-1$ 在内）。

- **（1）** $H(key)=key/n$。
- **（2）** $H(key)=1$。
- **（3）** $H(key)=(key+random(n))\%n$。
- **（4）** $H(key)=key\%p(n)$；

其中 $p(n)$ 是不大于 $n$ 的最大素数。

> [!note]- 原题截图
> ![[王道DS 7.5 散列表 综合 02.webp]]

> [!success]- 答案与解析
> **答案：**
>
> **（1）** $H(key)=key/n$ 不能作为该表的散列函数。整数关键字可能使 $key/n>=n$，函数值不一定落在地址范围 $[0,n-1]$ 内。
>
> **（2）** $H(key)=1$ 可以作为散列函数，但不是好的散列函数。它把所有关键字都映射到地址 1，会造成最严重的冲突。
>
> **（3）** $H(key)=(key+random(n))\%n$ 不能作为散列函数。同一关键字每次计算可能得到不同地址，不能保证插入与查找采用相同映射。
>
> **（4）** $H(key)=key\%p(n)$ 可以作为散列函数，而且通常是较好的散列函数。$p(n)$ 是不大于表长的最大素数，函数值一定落在合法地址范围内，除留余数法也有利于使关键字均匀分布。
>
> **解析：** 合格的散列函数至少要满足两点：输出地址合法；同一关键字的映射确定不变。进一步评价优劣时，还要看能否尽量均匀地使用全部散列地址。

## 关联

- 知识点：
- 题型：
