---
source: 王道DS
subject: DS
chapter: 07 查找
section: 7.2 顺序查找和折半查找
qtype: 综合
topic: []
answer_status: 已录入
tags: []
---
## 题干

6. 已知一个 $n$ 阶矩阵 $A$ 和一个目标值 $k$。

该矩阵无重复元素，每行从左到右升序排列，每列从上到下升序排列。

请设计一个在时间上尽可能高效的算法，判断矩阵中是否存在目标值 $k$。

例如，矩阵为

$$
\begin{bmatrix}
1&4&7\\
2&5&8\\
3&6&9
\end{bmatrix},
$$

目标值为 8，判断存在。

要求：

- **（1）** 给出算法的基本设计思想。
- **（2）** 根据设计思想，采用 C 或 C++ 语言描述算法，关键之处给出注释。
- **（3）** 说明你的算法的时间复杂度和空间复杂度。

> [!note]- 原题截图
> ![[王道DS 7.2 顺序查找和折半查找 综合 06.png]]

> [!success]- 答案与解析
> **答案：**
>
> 从矩阵右上角开始查找：
>
> ```c
> bool SearchMatrix(int A[][MAXN], int n, int k) {
>     int i = 0, j = n - 1;
>     while (i < n && j >= 0) {
>         if (A[i][j] == k) return true;
>         if (A[i][j] > k) --j;
>         else ++i;
>     }
>     return false;
> }
> ```
>
> 时间复杂度为 $O(n)$，额外空间复杂度为 $O(1)$。
>
> **解析：** 右上角元素是所在行的最大值、所在列的最小值。当它大于 $k$ 时可排除当前列，小于 $k$ 时可排除当前行。每步至少删去一行或一列，最多移动不超过 $2n-1$ 次。

## 关联

- 知识点：
- 题型：
