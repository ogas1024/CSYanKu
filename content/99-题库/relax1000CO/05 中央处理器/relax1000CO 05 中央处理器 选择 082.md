---
source: relax1000CO
subject: CO
chapter: 05 中央处理器
qtype: 选择
answer: A
answer_status: 已录入
topic: []
tags: []
---
## 题干

82. 关于硬件多线程的说法，错误的是（　）。

A．单核 CPU 同一时刻一定只有一个线程在执行

B．多核 CPU 使用多线程才能发挥硬件优势

C．超线程可以看作细粒度多线程和超标量处理器的结合

D．超线程支持指令级并行

> [!note]- 原题截图
> ![[relax1000CO 05 中央处理器 选择 082.webp]]

> [!success]- 答案与解析
> **答案：** A
>
> **解析：** A 错误（应选），单核 CPU 在支持超线程的情况下，同一时刻可以有多个线程"在执行"：超线程物理上只有一个核心，但逻辑上有多个，比如一个线程在 I/O 或访存等待时，可以执行另一个线程进行计算，两个线程不冲突，可进一步利用硬件资源。B 正确，多核 CPU 如果不使用多线程，那么一定有多个核心处于闲置状态，造成资源浪费。C 正确，SMT（同时多线程）技术充分利用了超标量处理器的多通道、动态指令调度和寄存器重命名机制，可以在一个周期里面发射来自不同线程的多个指令，可以看作细粒度多线程和超标量处理器的结合。D 正确，超线程是指令级并行，而粗/细粒度多线程仅支持线程级并行。

## 关联

- 知识点：
- 题型：
