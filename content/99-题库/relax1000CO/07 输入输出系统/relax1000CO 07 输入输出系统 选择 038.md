---
source: relax1000CO
subject: CO
chapter: 07 输入输出系统
qtype: 选择
answer: B
answer_status: 已录入
topic: []
tags: []
---
## 题干

38. 中断方式和 DMA 方式是两种典型的 I/O 控制方式。下列关于中断方式与 DMA 方式的说法，错误的是（　）。

A. 从本质上讲，中断方式以软件方式传输数据，DMA 方式以硬件方式传输数据

B. 当 CPU 运行中断处理程序时，若外设发出 DMA 请求信号，则 CPU 中断当前中断处理程序的运行，处理 DMA 请求，处理完毕后，继续运行原中断处理程序

C. 对中断请求的响应只能发生在每个指令周期结束时，对 DMA 请求的响应可以发生在每个总线事务结束后

D. 中断方式传输数据时需要保护原程序的现场，DMA 方式传输数据时无须保护现场

> [!note]- 原题截图
> ![[relax1000CO 07 输入输出系统 选择 038.webp]]

> [!success]- 答案与解析
> **答案：** B
>
> **解析：**
>
> 中断方式中由 CPU 运行中断处理程序完成数据传输（软件方式），DMA 方式由 DMA 控制器直接控制外设与主存之间的数据传输（硬件方式），A 正确。DMA 请求的响应优先级虽高于中断方式，但是否立即中断当前中断处理程序还需视 DMA 的传送方式而定（例如周期挪用方式下，若此时中断处理程序正在访存，则需等待该存取周期结束后才将总线占有权让给 DMA 控制器），并非简单地“中断当前中断处理程序”，B 错误。对外部中断的响应发生在每个指令周期结束之后，对 DMA 请求的响应发生在每个总线事务结束之后，C 正确。DMA 控制器是独立硬件，有自己独立的寄存器控制数据传输，传输数据时不需要 CPU 参与，也不需要修改 CPU 的现场信息；中断方式需要 CPU 运行中断服务程序完成传输，因此需要保存 CPU 的现场信息，D 正确。

## 关联

- 知识点：
- 题型：
