---
tags: [题目/综合, 408真题, OS/03-内存管理]
score: 11
year: 2014
---
## 题干

45. （11 分）假设对于 44 题中的计算机 M 和程序 P 的机器代码，M 采用页式虚拟存储管理；

P 开始执行时，$(R1)=(R2)=0$，$(R6)=1000$，其机器代码已调入主存但不在 Cache 中；

数组 A 未调入主存，且所有数组元素在同一页，并存储在磁盘同一个扇区。

请回答下列问题并说明理由。

1. P 执行结束时，R2 的内容是多少？

2. M 的指令 Cache 和数据 Cache 分离。

若指令 Cache 共有 16 行，Cache 和主存交换的块大小为 32 字节，则其数据区的容量是多少？若仅考虑程序段 P 的执行，则指令 Cache 的命中率为多少？

3. P 在执行过程中，哪条指令的执行可能发生溢出异常？哪条指令的执行可能产生缺页异常？对于数组 A 的访问，需要读磁盘和 TLB 至少各多少次？

> [!note]- 原题截图
> ![[408真题-2014-T45.png]]

> [!success]- 答案与解析
> **答案：**
>
> 1. 循环结束时 `(R2)=1000`。
> 2. 指令 Cache 数据区容量为 $16×32=512$ B。P 共 6 条指令、24 B，位于同一 32 B 主存块；仅首次取指缺失，命中率为 $(6000-1)/6000\approx99.98\%$。
> 3. 指令 4（`add R1,R1,R5`）可能发生溢出异常；指令 3（load）可能产生缺页异常。数组 A 全在同一页和同一磁盘扇区，不置换时至少读磁盘 1 次；首次访问 TLB 缺失并在缺页处理后重执行 load，随后 1000 次均命中，共查询 TLB 1001 次。
>
> **解析：** R2 逐次加 1 直到等于 N；指令块一次调入后持续命中。数组首次访问先查 TLB、触发缺页，处理后重执行并再次查 TLB。

## 关联

- 知识点：
- 题型：
