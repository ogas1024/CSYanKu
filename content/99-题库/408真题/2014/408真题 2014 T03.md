---
source: 408真题
subject: DS
chapter: 03 栈队列和数组
qtype: 选择
topic: []
answer: A
score: 2
year: 2014
answer_status: 已录入
tags: []
---
## 题干

3. 循环队列放在一维数组 $A[0\cdots M-1]$ 中，$end1$ 指向头元素，$end2$ 指向队尾元素的后一个位置。

假设队列两端均可进行入队和出队操作，队列中最多能容纳 $M-1$ 个元素。

初始时为空。

下列判断队空和队满的条件中，正确的是（）。

A. 队空：$end1==end2$；队满：$end1==(end2+1)\bmod M$

B. 队空：$end1==end2$；队满：$end2==(end1+1)\bmod(M-1)$

C. 队空：$end2==(end1+1)\bmod M$；队满：$end1==(end2+1)\bmod M$

D. 队空：$end1==(end2+1)\bmod M$；队满：$end2==(end1+1)\bmod(M-1)$

> [!note]- 原题截图
> ![[408真题 2014 T03.webp]]

> [!success]- 答案与解析
> **答案：** A
>
> **解析：** end1指向队头元素，end2指向队尾元素的后一个元素。队空时队头与队尾指针重合。队满时容纳M-1个元素，即空出一个元素用来判断队满，可得当end1==end2+1时为队满。又因为该队列为循环队列，则需要对m进行取余操作。故选A。
>
> 【点评】高频考点。此类题，考生可通过举一个具体的例子、画图辅助判断。一般循环队列初始为空都是设置为尾指针=头指针。循环队列判断队空和队满，有几种常见的处理方式，具体内容可以参考竟成《数据结构考研复习全书》相应章节。

## 关联

- 知识点：
- 题型：
