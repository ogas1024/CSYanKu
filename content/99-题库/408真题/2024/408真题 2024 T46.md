---
source: 408真题
subject: OS
chapter: 02 进程管理
qtype: 综合
topic: []
score: 8
year: 2024
answer_status: 已录入
tags: []
---
## 题干

46. （8分）计算机系统中的进程之间往往需要相互协作以完成一个任务。

在某网络系统中，缓冲区B用于存放一个数据分组，对B的操作有 $C_1$、$C_2$ 和 $C_3$。

$C_{1}$将一个数据分组写入B， $C_{2}$从B中读出一个数据分组， $C_{3}$对B中的数据分组进行修改。

要求B为空时才能执行 $C_{1}$，B非空时才能执行 $C_{2}$和 $C_{3}$。

请回答下列问题。

1）假设进程 P1 和 P2 均需要执行 C1，实现 C1 的代码是否为临界区？为什么？（2分）

2）假设 B 初始为空，进程 P1 执行 C1 一次，进程 P2 执行 C2 一次。

请定义尽可能少的信号量，并用 wait()、signal() 操作描述进程 P1 和 P2 之间的同步或互斥关系，说明所用信号量的作用及其初值。

（3分）

3）假设 B 初始不为空，进程 P1 和 P2 各执行 C3 一次。

请定义尽可能少的信号量，并用 wait()、signal() 操作描述进程 P1 和 P2 之间的同步或互斥关系，说明所用信号量的作用及其初值。

（3分）

> [!note]- 原题截图
> ![[408真题 2024 T46.png]]

> [!success]- 答案与解析
> **答案：**
>
> - **（1）** 是临界区。$C_1$ 会写共享缓冲区 B，P1 和 P2 执行 $C_1$ 时必须互斥。
> - **（2）** 只定义一个同步信号量 `S=0`：P1 执行 $C_1$ 后 `signal(S)`，P2 先 `wait(S)` 再执行 $C_2$。
> - **（3）** 只定义一个互斥信号量 `mutex=1`：P1、P2 都执行 `wait(mutex); C3; signal(mutex);`。
>
> **解析：** 第（2）中 B 初始为空且容量为 $1$，同步信号量本身已能保证先写后读及对 B 的互斥访问，无需另设互斥量。

## 关联

- 知识点：
- 题型：
