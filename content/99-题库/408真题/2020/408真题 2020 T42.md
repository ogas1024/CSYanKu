---
source: 408真题
subject: DS
chapter: 05 树与二叉树
qtype: 综合
topic: []
score: 10
year: 2020
answer_status: 已录入
tags: []
---
## 题干

42. （10分）若任一个字符的编码都不是其他字符编码的前缀，则称这种编码具有前缀特性。

现有某字符集（字符个数 $\geq 2$）的不等长编码，每个字符的编码均为二进制的0、1序列，最长为L位，且具有前缀特性。

请回答下列问题：

（1）哪种数据结构适宜保存上述具有前缀特性的不等长编码？

（2）基于你所设计的数据结构，简述从0/1串到字符串的译码过程。

（3）简述判定某字符集的不等长编码是否具有前缀特性的过程。

> [!note]- 原题截图
> ![[408真题 2020 T42.webp]]

> [!success]- 答案与解析
> **解析：**
>
> (1) 适宜用二叉树保存上述具有前缀特性的不等长编码。用二叉树中的叶结点保存字符，字符编码规则为：从根结点开始，左分支路径记为0，右分支路径记为1，从根结点到叶结点路径上、由0和1组成的字符串，即为该叶结点保存字符的编码。
>
> (2) 从左到右依次遍历 0/1 串中的各位。从二叉树的根结点开始，若 0/1 串中的当前位为 0，则查找左子树；若 0/1 串中的当前位为 1，则查找右子树，直到遍历到叶结点，输出叶结点中保存的字符，并从根结点开始重复此过程。直到遍历完整个 0/1 串，则译码完成。此过程中输出的字符串即为 0/1 串表示的字符串。
>
> (3) 这个过程描述很繁杂，这里直接原文摘抄官方给出的参考答案：
>
> 二叉树既可用于保存各字符的编码，也可用于检测编码是否具有前缀特性。判定编码是否具有前缀特性的过程，同时也是构建二叉树的过程。初始时，二叉树中仅含有根结点，其左子指针和右子指针均为空。依次读入每个编码 C，建立 / 寻找从根开始对应于该编码的一条路径，过程如下：对每个编码，从左至右扫描 C 的各位，根据 C 当前位（0 或 1）沿结点的指针（左子指针或右子指针）向下移动。当遇到空指针时，创建新结点，让为空的指针指向该新结点并继续移动。沿指针移动过程中，可能遇到三种情况：
>
> ①若遇到了叶结点（非根），则表明不具有前缀特性，返回；
>
> ②若在处理C的所有位的过程中，均没有创建新结点，则表明不具有前缀特性，返回；
>
> ③若处理C的最后一个编码位时创建了新结点，则继续验证下一个编码。
>
> 若所有编码均通过验证，则编码具有前缀特性。
>
> ▷【点评】创新题。这题考查前缀编码，在介绍哈夫曼编码时都会介绍这部分内容。第三小问的描述比较麻烦，比较难表述清楚。

## 关联

- 知识点：
- 题型：
