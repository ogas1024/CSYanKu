---
source: 408真题
subject: CO
chapter: 03 存储器层次结构
qtype: 综合
topic: []
score: 10
year: 2020
answer_status: 已录入
tags: []
---
## 题干

44. （10分）假定主存地址为32位，按字节编址，指令Cache和数据Cache与主存之间均采用8路组相联映射方式，直写（Write Through）写策略和LRU替换算法，主存块大小为64B，数据区容量各为32KB。

开始时Cache均为空。

请回答下列问题

（1）Cache 每一行中标记（Tag）、LRU 位各占几位？是否有修改位？

（2）有如下C语言程序段：

```c
for (k = 0; k < 1024; k++)
    s[k] = 2 * s[k];
```

若数组 s 及其变量 k 均为 int 型，int 型数据占 4B, 变量 k 分配在寄存器中，数组 s 在主存中的起始地址为 0080 00C0H, 则该程序段执行过程中，访问数组 s 的数据 Cache 缺失次数为多少？

（3）若 CPU 最先开始的访问操作是读取主存单元 0001 0003H 中的指令，简要说明从 Cache 中访问该指令的过程，包括 Cache 缺失处理过程。

> [!note]- 原题截图
> ![[408真题 2020 T44.png]]

> [!success]- 答案与解析
> **答案：**
>
> 1. 主存块为 64 B，块内地址 6 位；Cache 有 $32\,\mathrm{KB}/(64\,\mathrm{B}\times 8)=64$ 组，组号 6 位；标记为 $32-6-6=20$ 位。8 路 LRU 每行需 3 位替换信息；采用直写方式，无修改位。
> 2. 数组 s 共占 $1024\times 4=4096\,\mathrm{B}$，即 64 个主存块，且从块边界开始顺序读写。每块仅首次访问缺失，因此共缺失 64 次。
> 3. 地址 `00010003H` 的组索引为 0、标记为 `00010H`、块内偏移为 `000011B`。Cache 初始为空，访问缺失；把该主存块装入第 0 组任一行，写入标记并置有效位、更新 LRU 信息，再按块内偏移取数。
>
> **解析：** 地址按标记、组号、块内偏移三段划分。顺序处理对齐数组时，同一 64 B 块中的 16 个 int 只有第一次访问产生强制缺失。

## 关联

- 知识点：
- 题型：
