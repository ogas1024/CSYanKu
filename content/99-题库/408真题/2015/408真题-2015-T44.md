---
tags: [题目/综合, 408真题, CO/04-指令系统]
score: 10
year: 2015
---
## 题干

44. （10 分）题 43 中描述的计算机，其部分指令执行过程的控制信号如题 44 图 a 所示。

![[408真题-2015-T44-fig2.jpg|500]]

题 44 图 a 部分指令控制信号

该机指令格式如题 44 图 b 所示，支持寄存器直接和寄存器间接两种寻址方式，寻址方式位分别为 0 和 1，通用寄存器 R0～R3 的编号分别为 0、1、2 和 3。

![[408真题-2015-T44-fig1.jpg|500]]

题 44 图 b 指令格式

请回答下列问题。

- **（1）** 该机的指令系统最多可定义多少条指令？

- **（2）** 若 `inc`、`shl` 和 `sub` 指令的操作码分别为 `01H`、`02H` 和 `03H`，则以下指令对应的机器代码各是什么？

  ① `inc R1`；`R1+1 → R1`

  ② `shl R2, R1`；`(R1)<<1 → R2`

  ③ `sub R3, (R1), R2`；`((R1))-(R2) → R3`

- **（3）** 假设寄存器 X 的输入和输出控制信号分别为 `Xin` 和 `Xout`，其值为 1 表示有效，为 0 表示无效（如 `PCout=1` 表示 PC 内容送总线）；存储器控制信号为 `MEMop`，用于控制存储器的读（read）和写（write）操作。写出题图 a 中标号①～⑧处的控制信号或控制信号的取值。

- **（4）** 指令 `sub R1, R3, (R2)` 和 `inc R1` 的执行阶段至少各需要多少个时钟周期？

> [!note]- 原题截图
> ![[408真题-2015-T44.png]]

> [!success]- 答案与解析
> **答案：**
>
> 1. 7 位操作码最多表示 $2^7=128$ 条指令。
> 2. 机器代码：`inc R1` 为 `0240H`；`shl R2,R1` 为 `0488H`；`sub R3,(R1),R2` 为 `06EAH`。
> 3. 各空控制信号依次为：①0，②mov，③mova，④left，⑤read，⑥sub，⑦mov，⑧SRout。
> 4. `sub` 指令执行阶段至少 4 个时钟周期，`inc` 指令至少 2 个时钟周期。
>
> **解析：** 按指令格式拼接操作码与寄存器/寻址字段得到机器码；再把每条微操作映射到 ALU、移位器、存储器及总线门控信号。

## 关联

- 知识点：
- 题型：
