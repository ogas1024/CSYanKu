---
source: 408真题
subject: OS
chapter: 04 文件管理
qtype: 综合
topic: []
score: 7
year: 2022
answer_status: 已录入
tags: []
---
## 题干

45. （7分）某文件系统的磁盘块大小为4KB，目录项由文件名和索引结点号构成，每个索引结点占256字节，其中包含直接地址项10个，一级、二级和三级间接地址项各1个，每个地址项占4字节。

该文件系统中子目录stu的结构如题45(a)图所示，stu包含子目录course和文件doc，course子目录包含文件course1和course2。

各文件的文件名、索引结点号、占用磁盘块的块号如题45(b)图所示。

![[408真题 2022 T45-fig1.webp]]

题45(a)图

| 文件 | 索引结点号 | 磁盘块号 |
| --- | --- | --- |
| stu | 1 | 10 |
| course | 2 | 20 |
| course1 | 10 | 30 |
| course2 | 100 | 40 |
| doc | 10 | x |

题45(b)图

请回答下列问题。

（1）目录文件 stu 中每个目录项的内容是什么？

（2）文件 doc 占用的磁盘块的块号 x 的值是多少？

（3）若目录文件 course 的内容已在内存，则打开文件 course1 并将其读入内存，需要读几个磁盘块？说明理由。

（4）若文件 course2 的大小增长到 6 MB，则为了存取 course2 需要使用该文件索引结点的哪几级间接地址项？说明理由。

> [!note]- 原题截图
> ![[408真题 2022 T45.webp]]

> [!success]- 答案与解析
> **解析：**
>
> (1) 题目已说明“目录项由文件名和索引节点号”构成: 由图(a)可知, 文件 stu 包含子目录 course 和文件 doc; 由图(b)可知, 子目录 course 的索引节点号为 2, 文件 doc 的索引节点号为 10。所以, stu 的目录项内容如右表所示。
>
> (2) 由图(b)可知，doc 和 course1 的索引节点号相同，均为 10，说明两者共用一个索引节点，对应同一个文件。由图(b)可知，course1 的磁盘块号是 30，因此 doc 的磁盘块号也是 30，即 x 的值为 30。
>
> | 文件名 | 索引节点号 |
> |---|---|
> | course | 2 |
> | doc | 10 |
>
> (3) 需要读2个磁盘块。读取的第一个磁盘块是 course1 的索引节点所在的磁盘块，读取的第二 个磁盘块是 course1 的内容所在的磁盘块。理由如下：题目已说明“目录文件 course 的内容已在内存”，由图 (a) 可知，目录文件 course 的内容包含 2 个目录项：文件 course1 的目录项和文件 course2 的目录项。由图 (b) 可知，文件 course1 的索引节点号是 10，先将 10 号索引节点所在的磁盘块读入内存，根据 10 号索引节点中的内容可得到文件的磁盘块号是 30，再将 30 号磁盘块读入内存获取文件 course1 的内容。
>
> (4) 存取 course2 需要使用索引节点的一级和二级间接地址项。10 个直接地址项记录的盘块总共可存储  $4KB \times 10 = 40KB$，一级间接地址记录的盘块总共可存储  $4KB \div 4B \times 4KB = 2^{10} \times 4KB = 4MB$，二级间接地址项记录的盘块总共可存储  $4KB \div 4B \times 4MB = 2^{10} \times 4MB = 4GB$。因为  $40KB + 4MB < 6MB < 40KB + 4MB + 4GB$，所以要用到一级间接地址项和二级间接地址项。
>
> 【点评】常规题。考查文件相关的常见概念，在教辅书中也有大量类似的练习题。

## 关联

- 知识点：
- 题型：
