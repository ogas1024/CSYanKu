---
tags: [题目/综合, 408真题, CO/06-总线和输入输出系统]
score: 8
year: 2022
---
## 题干

44. （8分）假设某磁盘驱动器中有4个双面盘片，每个盘面有20000个磁道，每个磁道有500个扇区，每个扇区可记录512字节的数据，盘片转速为7200 r/m（转/分），平均寻道时间为5ms。

请回答下列问题：

（1）每个扇区包含数据及其地址信息，地址信息分为3个字段。

这3个字段的名称各是什么？对于该磁盘，各字段至少占多少位？

（2）一个扇区的平均访问时间约为多少？

（3）若采用周期挪用 DMA 方式进行磁盘与主机之间的数据传送，磁盘控制器中的数据缓冲区大小为 64 位，则在一个扇区读写过程中，DMA 控制器向 CPU 发送了多少次总线请求？若 CPU 检测到 DMA 控制器的总线请求信号时也需要访问主存，则 DMA 控制器是否可以获得总线使用权？为什么？

> [!note]- 原题截图
> ![[408真题-2022-T44.png]]

> [!success]- 答案与解析
> **答案：**
>
> 1. 地址字段为柱面号（磁道号）、磁头号（盘面号）和扇区号，至少分别占 15、3、9 位。
> 2. 转一圈时间为 $60\times10^3/7200\approx8.33$ ms；平均访问时间为 $5+8.33/2+8.33/500\approx9.18$ ms。
> 3. 一个扇区需总线请求 $512\text{ B}/(64\text{ bit})=64$ 次。采用周期挪用 DMA 时，DMA 控制器可优先取得总线；磁盘数据必须按时传走，否则会丢失。
>
> **解析：** 4 个双面盘片给出 8 个盘面，20000 个柱面、每磁道 500 个扇区。平均访问时间由平均寻道、半圈旋转延迟和一个扇区传输时间组成。

## 关联

- 知识点：
- 题型：
