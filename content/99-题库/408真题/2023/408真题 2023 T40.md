---
source: 408真题
subject: CN
chapter: 04 网络层
qtype: 选择
topic: []
answer: D
score: 2
year: 2023
answer_status: 已录入
tags: []
---
## 题干

40. 下列关于 IPv6 和 IPv4 的叙述中，正确的是（）。

I. IPv6 地址空间是 IPv4 地址空间的 96 倍

II. IPv4 和 IPv6 的基本首部的长度均可变

III. IPv4 向 IPv6 过渡可以采取双协议栈和隧道技术

IV. IPv6 首部的 Hop Limit 等价于 IPv4 首部的 TTL 字段

A. I、II

B. I、IV

C. II、III

D. III、IV

> [!note]- 原题截图
> ![[408真题 2023 T40.webp]]

> [!success]- 答案与解析
> **答案：** D
>
> **解析：** ①I 错误 ×：IPv6 的地址长度为 128 位，IPv4 的地址长度为 32 位，IPv6 的地址空间是 IPv4 地址空间的  $2^{128} \div 2^{32} = 2^{96}$ 倍。
>
> ②Ⅱ错误 x：IPv4 基本首部长度是可变的（但必须是 4 字节的倍数）：包含 20 字节的固定部分（用于传输和路由数据包）和最多 40B 的可选部分。IPv6 基本首部长度固定为 40B。
>
> ③ III 正确 ✓：双协议栈（Dual Stack）是指在同一设备上同时支持 IPv4 和 IPv6 协议栈。设备可以根据需要选择使用 IPv4 和 IPv6 两种协议进行通信。隧道技术（Tunneling）是一种将 IPv6 数据包封装在 IPv4 数据包中进行传输的方法，使得 IPv6 数据可以通过 IPv4 网络进行传输，实现 IPv4 到 IPv6 的过渡。
>
> ④ IV 正确 ✓：Hop-Limit 字段和 TTL 字段的作用都是限制数据报在网络中的传输跳数（即经过的路由器数量）。每经过一个路由器，跳数值就会减少。当跳数值达到 0 时，数据报会被丢弃，并发送一个 ICMP（Internet Control Message Protocol）时间超时报文。故选 D。
>
> 【点评】常规题。考查基本概念，记忆性的内容。

## 关联

- 知识点：
- 题型：
