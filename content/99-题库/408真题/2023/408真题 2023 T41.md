---
source: 408真题
subject: DS
chapter: 06 图
qtype: 综合
topic: []
score: 13
year: 2023
answer_status: 已录入
tags: []
---
## 题干

41. （13分）有向图采用邻接矩阵存储，其定义如下：

```c
typedef struct {                     // 图的类型定义
    int numVertices, numEdges;        // 图的顶点数和有向边数
    char VerticesList[MAXV];          // 顶点表，MAXV 为已定义常量
    int Edge[MAXV][MAXV];             // 邻接矩阵
} MGraph;
```

将图中出度大于入度的顶点称为 K 顶点。

例如：在下图中，顶点 a 和顶点 b 都是 K 顶点。

![[408真题 2023 T41-fig1.webp]]

请设计算法：`int printVertices(MGraph G)`，对给定的任意非空有向图 G，输出图 G 中所有的 K 顶点，并返回 K 顶点的个数。

要求：

（1）给出算法的设计思想。

（2）根据算法思想，采用 C 或 C++ 语言描述算法，并在关键之处给出注释。

> [!note]- 原题截图
> ![[408真题 2023 T41.webp]]

> [!success]- 答案与解析
> **解析：**
>
> (1) 算法设计思想：
>
> (a) 初始化计数器 cnt 为 0，用于记录 K 顶点的个数。
>
> (b) 遍历图中的每个顶点 v:
>
> I. 初始化 inDegree 和 outDegree 为 0，分别表示顶点 v 的入度和出度。
>
> Ⅱ. 遍历图中的每个顶点 u：如果边 (u, v) 存在，即 G.Edge[u][v] 为 1，则将 outDegree 增加 1。如果边 (v, u) 存在，即 G.Edge[v][u] 为 1，则将 inDegree 增加 1。
>
> III. 如果 outDegree 大于 inDegree，则顶点 v 是一个 K 顶点。输出顶点 v，将 cnt 增加 1。
>
> (c) 返回 cnt 作为 K 顶点的个数。
>
> (2) 算法实现如下：
>
> int printVertices(MGraph G) { int cnt = 0; // 计数器，记录K顶点的个数 for (int v = 0; v < G.numVertices; v++) { // 遍历每个顶点 int inDegree = 0; // 记录顶点v的入度 int outDegree = 0; // 记录顶点v的出度 for (int u = 0; u < G.numVertices; u++) { // 遍历每个顶点 if (G.Edge[u][v] == 1) // 边(u, v)存在，顶点v的入度加1 inDegree++; if (G.Edge[v][u] == 1) // 边(v, u)存在，顶点v的出度加1 outDegree++; } if (outDegree > inDegree) { // 如果出度大于入度，则是K顶点 printf("%c ", G.VerticesList[v]); // 输出顶点v cnt++; // K顶点的个数加1 } } return cnt; // 返回K顶点的个数 }
>
> 【真题溯源】本题本质上是有向图邻接矩阵求度的基础操作题，是数据结构教材级别的入门题，LeetCode 没有对应的原型题。“出度大于入度的顶点称为 K 顶点”这个定义是命题组自定义的。
>
> ▷【点评】常规题。命题人在本题难度设置上比较克制。常见知识点的常规考法。通过两层循环遍历邻接矩阵解决问题。而且，本题与2021年408第41题很相似，考法和考点都是一样的。对于考研这种选拔性考试来说，考生如果在这类题上丢分，影响还是很大的。因为这类常见知识点的常规考法、而且往年还考过类似的题，一般只要认真复习的考生都能做对，只要出错就跟竞争对手拉开了十分以上的差距。难题、冷门考点或者之前没考过的创新题目，可能大家第一次遇到都不一定能做对，彼此之间难以拉开差距。所以，备考时，先掌握常规考点，重难点，熟练掌握以前408真题中考过的题，再去扩展学习冷门考点。要分清主次，不能本末倒置。

## 关联

- 知识点：
- 题型：
