---
source: 408真题
subject: DS
chapter: 01 绪论
qtype: 综合
topic: []
score: 13
year: 2013
answer_status: 已录入
tags: []
---
## 题干

41. （13 分）已知一个整数序列 $A=(a_0,a_1,\cdots,a_{n-1})$，其中 $0\leq a_i<n$（$0\leq i<n$）。

若存在 $a_{p_1}=a_{p_2}=\cdots=a_{p_m}=x$ 且 $m>n/2$（$0\leq p_k<n,1\leq k\leq m$），则称 $x$ 为 $A$ 的主元素。

例如 $A=(0,5,5,3,5,7,5,5)$，则 $5$ 为主元素；又如 $A=(0,5,5,3,5,1,5,7)$，则 $A$ 中没有主元素。

假设 $A$ 中的 $n$ 个元素保存在一个一维数组中，请设计一个尽可能高效的算法，找出 $A$ 的主元素。

若存在主元素，则输出该元素；否则输出 $-1$。

要求：

（1）给出算法的基本设计思想。
（2）根据设计思想，采用 C、C++ 或 Java 语言描述算法，关键之处给出注释。
（3）说明你所设计算法的时间复杂度和空间复杂度。

> [!note]- 原题截图
> ![[408真题 2013 T41.png]]

> [!success]- 答案与解析
> **答案：**
>
> 1. 用 Boyer–Moore 投票法扫描数组：相同元素计数加 1，不同元素抵消减 1；计数归零时更换候选。再扫描一次确认候选出现次数是否超过 n/2。
> 2. ```c
> int Majority(int A[], int n) {
>     int c=A[0], count=1;
>     for (int i=1; i<n; ++i) {
>         if (A[i]==c) ++count;
>         else if (count>0) --count;
>         else { c=A[i]; count=1; }
>     }
>     count=0;
>     for (int i=0; i<n; ++i) if (A[i]==c) ++count;
>     return count>n/2 ? c : -1;
> }
> ```
> 3. 时间复杂度 O(n)，空间复杂度 O(1)。
>
> **解析：** 主元素若存在，不同元素两两抵消后仍会留下它；首次扫描只产生候选，因此必须二次计数验证。

## 关联

- 知识点：
- 题型：
