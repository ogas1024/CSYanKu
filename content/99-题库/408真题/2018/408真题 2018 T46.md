---
source: 408真题
subject: OS
chapter: 04 文件管理
qtype: 综合
topic: []
score: 7
year: 2018
answer_status: 已录入
tags: []
---
## 题干

46. （7分）某文件系统采用索引节点存放文件的属性和地址信息，簇大小为4KB。

每个文件索引节点占64B，有11个地址项，其中直接地址项8个，一级、二级和三级间接地址项各1个，每个地址项长度为4B。

请回答下列问题。

（1）该文件系统能支持的最大文件长度是多少？（给出计算表达式即可。）

（2）文件系统用 1M（1M=$2^{20}$）个簇存放文件索引节点，用 512M 个簇存放文件数据。

若一个图像文件的大小为 5600B，则该文件系统最多能存放多少个图像文件？

（3）若文件 F1 的大小为 6KB，文件 F2 的大小为 40KB，则该文件系统获取 F1 和 F2 最后一个簇的簇号需要的时间是否相同？为什么？

> [!note]- 原题截图
> ![[408真题 2018 T46.webp]]

> [!success]- 答案与解析
> **解析：**
>
> (1) 簇大小 4KB，每个地址项 4B，因此每个间接地址块可存放  $4KB \div 4B = 1024$ 个簇号。
>
> ◇ 直接地址：8 个簇  $\rightarrow$  $8 \times 4KB$;
>
> ◇ 一级间接：1024 个簇 → 1024 × 4KB
>
> ◇ 二级间接： $1024 \times 1024$ 个簇  $\rightarrow 1024^{2} \times 4KB$
>
> ◇ 三级间接： $1024 \times 1024 \times 1024$ 个簇  $\rightarrow 1024^3 \times 4\text{KB}$
>
> 最大文件长度 = (8 + 1024 + 1024^2 + 1024^3) × 4KB = 32KB + 4MB + 4GB + 4TB。
>
> (2) ① 索引节点区：1M 个簇，每簇 4KB，每个索引节点 64B，每簇可存 4KB ÷ 64B = 64 个索引节点，总索引节点数 = 1M × 64 = 64M 个。
>
> 182
>
> ② 数据区：每个图像文件 5600B，每簇 4KB = 4096B，存储一个图像文件需要  $[5600 \div 4096] = 2$ 个簇。512M 个簇最多可存放  $512M \div 2 = 256M$ 个图像文件。
>
> 最多可存放的文件数受限于索引节点数和数据簇数中的较小值= $\min(64M,256M)=64M$个。
>
> (3) ① F1 大小 6KB，占  $[6KB \div 4KB] = 2$ 个簇。2 $\le$ 8，两个簇全部由直接地址项索引。获取最后一个簇（第2簇）的簇号：只需读入 F1 的索引节点，从第2个直接地址项中即可得到，访问磁盘1次（读索引节点）。
>
> ② F2 大小 40KB，占  $[40KB \div 4KB] = 10$ 个簇。直接地址项只能覆盖前 8 簇，第 9、10 簇在一级间接索引范围。获取最后一个簇（第 10 簇）的簇号：先读索引节点得到一级间接地址项，再读该间接索引块，取出其中第 2 个地址项，共访问磁盘 2 次。
>
> ▷【点评】常规题。考查索引节点（inode）文件系统的地址结构和容量计算，此类题型是408操作系统部分的经典题型，解题套路固定，在此前的408真题中也考过很多次，但更多在选择题中考查。

## 关联

- 知识点：
- 题型：
