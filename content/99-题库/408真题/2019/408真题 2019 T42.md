---
source: 408真题
subject: DS
chapter: 03 栈队列和数组
qtype: 综合
topic: []
score: 10
year: 2019
answer_status: 已录入
tags: []
---
## 题干

42. （10分）请设计一个队列，要求满足：①初始时队列为空；

②入队时，允许增加队列占用空间；

③出队后，出队元素所占用的空间可重复使用，即整个队列所占用的空间只增不减；

④入队操作和出队操作的时间复杂度始终保持为O(1)。

请回答下列问题：

（1）该队列是应选择链式存储结构，还是应选择顺序存储结构？

（2）画出队列的初始状态，并给出判断队空和队满的条件。

（3）画出第一个元素入队后的队列状态。

（4）给出入队操作和出队操作的基本过程。

> [!note]- 原题截图
> ![[408真题 2019 T42.webp]]

> [!success]- 答案与解析
> **解析：**
>
> (1) 采用链式存储结构（循环单链表）。
>
> (2) 队列初始状态如下左图所示。以 front 为队头，rear 为队尾，队空的条件为 front == rear，队满的条件为 front == rear -> next。
>
> 队列初始状态
>
> 第一个元素入队后的队列状态
>
> (3) 插入第一个元素后的状态如上右图所示。
>
> (4) 入队和出队操作的伪代码描述如下：
>
> ① 入队操作的过程描述：先判断队列的状态。如果队列满（front == rear->next），队列中已
>
> 经没有空结点，则需要新建一个结点，把结点插入到队尾 rear 之后。用 e 表示入队元素，令：
>
> rear -> data = e; rear = rear -> next。
>
> ② 出队操作的过程描述：判断队列的状态。如果队列空（front == rear），队列中没有可以出队的元素，出队失败，返回空值。用 e 表示出队元素，令：e = front -> data; front = front -> next。最后返回 e。
>
> 【点评】常规题。考查队列，比较有新意。此题看似简单，实际上想获得满分则比较困难，考生动笔写后可能会遇见各种各样的问题，也可能在某些方面思考不完善。此题的启示：对于课本上有的基本数据结构，考生需要动手实现一遍。

## 关联

- 知识点：
- 题型：
