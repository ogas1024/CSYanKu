---
tags: [题目/综合, 408真题, OS/03-内存管理]
score: 7
year: 2016
---
## 题干

45. 某计算机采用页式虚拟存储管理方式，按字节编址，虚拟地址为 32 位，物理地址为 24 位，页大小为 8KB；

TLB 采用全相联映射；

Cache 数据区大小为 64KB，按 2 路组相联方式组织，主存块大小为 64B。

存储访问过程的示意图如下。

![[408真题-2016-T45-fig1.jpg]]

请回答下列问题。

（1）图中字段 A～G 的位数各是多少？TLB 标记字段 B 中存放的是什么信息？

（2）将块号为 4099 的主存块装入到 Cache 中时，所映射的 Cache 组号是多少？对应的 H 字段内容是什么？

（3）Cache缺失处理的时间开销大还是缺页处理的时间开销大？为什么？

（4）为什么 Cache 可以采用直写（Write Through）策略，而修改页面内容时总是采用回写（WriteBack）策略。

> [!note]- 原题截图
> ![[408真题-2016-T45.png]]

> [!success]- 答案与解析
> **答案：**
>
> 1. 字段位数：A=19、B=19、C=11、D=13、E=9、F=9、G=6；TLB 标记 B 存虚页号。
> 2. 主存块 4099 映射到 Cache 第 3 组，H 字段为 `000001000B`。
> 3. Cache 缺失开销小于缺页，因为前者只访问主存，后者需访问磁盘。
> 4. Cache—主存可用直写；主存—外存修改页面应采用回写。
>
> **解析：** 8 KB 页给 13 位页内偏移；64 B 块给 6 位块内偏移，64 KB、2 路 Cache 得 512 组。不同层次写入速度差异决定写策略。

## 关联

- 知识点：
- 题型：
