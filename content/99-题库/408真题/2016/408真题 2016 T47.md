---
source: 408真题
subject: OS
chapter: 04 文件管理
qtype: 综合
topic: []
score: 9
year: 2016
answer_status: 已录入
tags: []
---
## 题干

47. 某磁盘文件系统使用链接分配方式组织文件，簇大小为 4KB。

目录文件的每个目录项包括文件名和文件的第一个簇号，其他簇号存放在文件分配表 FAT 中。

（1）假定目录树如下图所示，各文件占用的簇号及顺序如下表所示，其中 dir、dir1 是目录，file1、file2 是用户文件。

请给出所有目录文件的内容。

![[408真题 2016 T47-fig1.webp]]

| 文件名 | 簇号 |
| --- | --- |
| dir | 1 |
| dir1 | 48 |
| file1 | 100、106、108 |
| file2 | 200、201、202 |

（2）若 FAT 的每个表项仅存放簇号，占 2 字节，则 FAT 的最大长度为多少字节？该文件系统支持的文件长度最大是多少？

（3）系统通过目录文件和 FAT 实现对文件的按名存取，说明 file1 的 106、108 两个簇号分别存放在 FAT 的哪个表项中。

（4）假设仅 FAT 和 dir 目录文件已读入内存，若需将文件 dir/dir1/file1 的第 5000 个字节读入内存，则要访问哪几个簇？

> [!note]- 原题截图
> ![[408真题 2016 T47.webp]]

> [!success]- 答案与解析
> **解析：**
>
> (1) dir 和 dir1 的内容如下所示：
>
> | 文件名 | 簇号 |
> |---|---|
> | dir1 | 48 |
> | dir 目录文件 | dir 目录文件 |
>
> | 文件名 | 簇号 |
> |---|---|
> | file1 | 100 |
> | file2 | 200 |
>
> dir1 目录文件
>
> (2) FAT 的每个表项仅存放簇号，占 2B = 16b，在 FAT 中最多有  $2^{16}$ 个表项，即  $2^{16}$ 个簇。FAT 的最大长度为  $2^{16} \times 2B = 128KB$。文件的最大长度为  $2^{16} \times 4KB = 256MB$。
>
> (3) 按簇分配的文件中，FAT 的每个表项存放文件的下一簇号，file1 的 106 簇号放在 FAT 的 100 号表项中，108 簇号放在 FAT 的 106 号表项中。
>
> (4) 首先在 dir 目录文件中找到 dir1 的簇号 48，读取 48 号簇，找到 dir1 目录文件；在 dir1 目录文件中找到 file1 文件的第一个簇号 100，无需读取 100 号簇，只需根据此簇号在 FAT 表中找到第 5000 个字节所在的簇号，由于一个簇为 4KB = 4096B，可知 5000B 在文件的第二个簇即 106 号簇。所以需要访问目录文件 dir1 的 48 号簇，和文件 file1 的 106 号簇。
>
> 【点评】常规题。考查 FAT 文件系统。考生须了解 FAT 和 UFS 两种文件系统的结构，可以结合教材教辅，自己画一个 UFS 或 FAT 文件系统示意图，并尝试访问某个目录下的文件，看看需经过哪些操作。这样考生会更清楚 FAT 系统和 UFS 系统各自的特点。例如 FAT 系统中，若要将某个文件读入内存，需要先将系统的 FAT 表读入内存，在 FAT 表中通过层层静态指针找到目标文件的表项，从而将目标文件读入内存；而对于 UFS 系统，在读入某个目录（除根目录外）或某个文件之前，要先将该文件的索引节点读入内存，在索引节点中查找目标块所在位置，再去磁盘中读取。
>
> 2016 年408真题分析
>
> 1. 数据结构（45分）
>
> ①选择题部分：第3题和第7题较难，其他都是常规题型，所以考生应该能拿 $9 \times 2 = 18$分。
>
> ②应用题部分：第42题考查树的算法推导，比较基础，这道题按得6分估算。第43题，考生如果会快速排序，即使拿不到满分，也应该能拿大半分数，这道题按得8分估算。
>
> 综上，复习到位的话，数据结构部分应该能拿 $18+14=32$分。
>
> 2. 计算机组成原理（45分）
>
> ① 选择题部分：除第20题选项设计迷惑性强、需要推理分析，容易出错。其他的10道选择题都是很常规的常见考点，考生应该能拿 $10\times2=20$分。
>
> ②应用题部分：第44题第(2)小问有难度，按得5分估算。第45题全是基础题型，考生应该能拿大半分数，按得10分估算。
>
> 综上，复习到位的话，计组部分应该能拿 $20+15=35$分。
>
> 3. 操作系统（35分）
>
> ①选择题部分：第25题、第30题、第32题比较难分析，其他都是常规题型，考生应该要能拿 $7 \times 2 = 12$分。
>
> ②应用题部分：第46题，考生应该可以做出来，但可能书写不规范，按得5分估算。第47题前两问比较基础，后两问有一定难度，应该能拿一半多分数，按拿5分估算。
>
> 综上，复习到位的话，操作系统部分应该能拿 $12+10=22$分。
>
> 4. 计算机网络（25分）
>
> ① 选择题部分：2016年408计网的难度很大，而且，计网的所有题目都是共用同一个背景进行分析，这也是408考试中首次出现这种考查方式。选择题第35、36、37、38、39题，这5题都是难题。其他3个选择题考生应该能拿 $3\times2=6$分。
>
> ②应用题部分：第41题很难，拥塞窗口大小的分析较为复杂，按拿4分估算。
>
> 综上，复习到位的话，计算机网络部分应该能拿 $6+4=10$分。
>
> 综上所述，本套试卷，考生如果能考到  $32+35+22+10=99$ 分，就算是不错的成绩了！

## 关联

- 知识点：
- 题型：
