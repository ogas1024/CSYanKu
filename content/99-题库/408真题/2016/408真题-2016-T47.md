---
tags: [题目/综合, 408真题, OS/04-文件管理]
score: 9
year: 2016
---
## 题干

47. 某磁盘文件系统使用链接分配方式组织文件，簇大小为 4KB。

目录文件的每个目录项包括文件名和文件的第一个簇号，其他簇号存放在文件分配表 FAT 中。

（1）假定目录树如下图所示，各文件占用的簇号及顺序如下表所示，其中 dir、dir1 是目录，file1、file2 是用户文件。

请给出所有目录文件的内容。

![[408真题-2016-T47-fig1.jpg]]

| 文件名 | 簇号 |
| --- | --- |
| dir | 1 |
| dir1 | 48 |
| file1 | 100、106、108 |
| file2 | 200、201、202 |

（2）若 FAT 的每个表项仅存放簇号，占 2 字节，则 FAT 的最大长度为多少字节？该文件系统支持的文件长度最大是多少？

（3）系统通过目录文件和 FAT 实现对文件的按名存取，说明 file1 的 106、108 两个簇号分别存放在 FAT 的哪个表项中。

（4）假设仅 FAT 和 dir 目录文件已读入内存，若需将文件 dir/dir1/file1 的第 5000 个字节读入内存，则要访问哪几个簇？

> [!note]- 原题截图
> ![[408真题-2016-T47.png]]

> [!success]- 答案与解析
> **答案：**
>
> 1. dir 目录含 `dir1→簇48`；dir1 目录含 `file1→簇100`、`file2→簇200`。
> 2. FAT 最大长度 128 KB，文件最大长度 256 MB。
> 3. file1 的簇 106 存在 FAT[100]，簇 108 存在 FAT[106]。
> 4. 查找 file1 第 5000 字节需访问 dir1 的簇 48 以及 file1 的簇 106。
>
> **解析：** 16 位簇号使 FAT 最多 $2^{16}$ 项，每簇 4 KB。目录项给首簇号，后续簇号存于当前簇对应 FAT 表项。

## 关联

- 知识点：
- 题型：
