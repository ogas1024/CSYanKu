---
tags: [题目/综合, 408真题, OS/04-文件管理]
score: 8
year: 2012
---
## 题干

46. （8 分）某文件系统空间的最大容量为 $4\,\mathrm{TB}$（$1\,\mathrm{TB}=2^{40}\,\mathrm{B}$），以磁盘块为基本分配单位。

磁盘块大小为 $1\,\mathrm{KB}$。

文件控制块（FCB）包含一个 $512\,\mathrm{B}$ 的索引表区。

请回答下列问题。

- **（1）** 假设索引表区仅采用直接索引结构，索引表区存放文件占用的磁盘块号，索引表项中块号最少占多少字节？可支持的单个文件最大长度是多少字节？
- **（2）** 假设索引表区采用如下结构：第 $0\sim7$ 字节采用 $\langle\text{起始块号，块数}\rangle$ 格式表示文件创建时预分配的连续存储空间，其中起始块号占 $6\,\mathrm{B}$，块数占 $2\,\mathrm{B}$；剩余 $504$ 字节采用直接索引结构，一个索引项占 $6\,\mathrm{B}$，那么可支持的单个文件最大长度是多少字节？为了使单个文件的长度达到最大，请指出起始块号和块数分别所占字节数的合理值并说明理由。

> [!note]- 原题截图
> ![[408真题-2012-T46.png]]

> [!success]- 答案与解析
> **答案：**
>
> 1. 4 TB/1 KB=$2^{32}$ 个块，块号至少 4 B；512 B 可放 128 个直接索引项，最大文件长度为 $128×1$ KB=128 KB。
> 2. 题设 6 B 起始块号、2 B 块数时，连续区最多 $2^{16}$ 块，直接索引区有 $(504/6)=84$ 项，最大长度为 64 MB+84 KB=65620 KB。若要使单文件达到系统上限 4 TB，起始块号和块数字段各取 4 B（其他 1/7、2/6、3/5 的合理拆分也可），使字段均能覆盖系统所需范围。
>
> **解析：** 直接索引上限由索引项数决定；混合结构的文件长度为预分配连续区加直接索引区，两字段需在寻址范围和可表示块数间平衡。

## 关联

- 知识点：
- 题型：
