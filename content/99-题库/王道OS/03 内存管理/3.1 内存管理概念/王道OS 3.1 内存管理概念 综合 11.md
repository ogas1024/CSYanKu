---
source: 王道OS
subject: OS
chapter: 03 内存管理
section: 3.1 内存管理概念
qtype: 综合
topic: []
answer_status: 已录入
tags: []
---
## 题干

某计算机主存按字节编址，逻辑地址和物理地址都是 32 位，页表项大小为 4B。

请回答下列问题：

- **（1）** 若使用一级页表的分页存储管理方式，逻辑地址结构为

| 页号（20 位） | 页内偏移量（12 位） |
| --- | --- |
|  |  |

则页的大小是多少字节？页表最大占用多少字节？

- **（2）** 若使用二级页表的分页存储管理方式，逻辑地址结构为

| 页目录号（10 位） | 页表索引（10 位） | 页内偏移量（12 位） |
| --- | --- | --- |
|  |  |  |

设逻辑地址为 LA，请分别给出其对应的页目录号和页表索引的表达式。

- **（3）** 采用1)中的分页存储管理方式，一个代码段的起始逻辑地址为0000 8000H，其长度为8KB，被装载到从物理地址0090 0000H开始的连续主存空间中。

页表从主存0020 0000H开始的物理地址处连续存放，如下图所示(地址大小自下向上递增)。

请计算出该代码段对应的两个页表项的物理地址、这两个页表项中的页框号，以及代码页面2的起始物理地址。


> [!note]- 原题截图
> ![[王道OS 3.1 内存管理概念 综合 11.png]]

> [!success]- 答案与解析
> **答案：**
>
> 1. 页内偏移12位，所以页面大小为 `2^12B=4KB`。页号20位，一级页表最多有 `2^20` 项，每项4B，最大占用4MB。
>
> 2. 页目录号可写为 `((unsigned int)LA >> 22) & 0x3FF`（或 `LA/2^22`）；页表索引可写为 `((unsigned int)LA >> 12) & 0x3FF`（或 `(LA/2^12) mod 2^10`）。
>
> 3. 代码段从逻辑页8开始，占逻辑页8、9。对应两页表项的物理地址分别为 `0020 0020H`、`0020 0024H`；页框号分别为 `00900H`、`00901H`；代码页面2的起始物理地址为 `0090 1000H`。
>
> **解析：** 每个页表项4B，页8和页9的表项地址分别是页表基址加8×4和9×4；代码被连续装入，第二页位于首物理页框的下一页框。

## 关联

- 知识点：
- 题型：
