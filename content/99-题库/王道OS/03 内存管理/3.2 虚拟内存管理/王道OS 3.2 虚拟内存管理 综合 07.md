---
source: 王道OS
subject: OS
chapter: 03 内存管理
section: 3.2 虚拟内存管理
qtype: 综合
topic: []
answer_status: 已录入
tags: []
---
## 题干

已知系统为 32 位实地址，采用 48 位虚拟地址，页面大小为 4KB，页表项大小为 8B，每段最大为 4GB。

- **（1）** 假设系统使用纯页式存储，则要采用多少级页表？页内偏移多少位？

- **（2）** 假设系统采用一级页表，TLB 命中率为 98%，TLB 访问时间为 10ns，内存访问时间为 100ns，并假设当 TLB 访问失败时才开始访问内存，问平均页面访问时间是多少？

- **（3）** 若是二级页表，页面平均访问时间是多少？

- **（4）** 上题中，若要满足访问时间小于 120ns，则命中率至少需要为多少？

- **（5）** 若系统采用段页式存储，则每用户最多可以有多少个段？段内采用几级页表？

> [!note]- 原题截图
> ![[王道OS 3.2 虚拟内存管理 综合 07.png]]

> [!success]- 答案与解析
> **答案：**
>
> 1. 4KB页给12位偏移，48位地址剩36位虚页号；每页可放512个8B页表项，即每级索引9位，需4级页表。
> 2. 一级页表、TLB命中率98%：平均访问时间 `0.98×(10+100)+0.02×(10+100+100)=112ns`。
> 3. 二级页表时为 `0.98×110+0.02×310=114ns`。
> 4. 要求 `p×110+(1−p)×310≤120`，得命中率 `p≥95%`。
> 5. 每段最大4GB，段内地址32位，段号16位，每用户最多2^16段；段内页号20位，以9位/级索引需3级页表。
>
> **解析：** TLB命中保存完整虚页号到页框映射，不随多级页表增加命中路径访存次数。

## 关联

- 知识点：
- 题型：
