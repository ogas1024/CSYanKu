---
source: 王道OS
subject: OS
chapter: 04 文件管理
section: 4.2 目录与文件
qtype: 综合
topic: []
answer_status: 已录入
tags: []
---
## 题干

某文件系统空间的最大容量为 $4\text{ TB}$（$1\text{ TB}=2^{40}\text{ B}$），以磁盘块为基本分配单位。

磁盘块大小为 1 KB。

文件控制块（FCB）包含一个 512 B 的索引表区。

请回答下列问题：

- **（1）** 假设索引表区仅采用直接索引结构，索引表区存放文件占用的磁盘块号，索引表项中块号最少占多少字节？可支持的单个文件的最大长度是多少字节？
- **（2）** 假设索引表区采用如下结构：第 0～7 字节采用〈起始块号，块数〉格式表示文件创建时预分配的连续存储空间。

其中起始块号占 6 B，块数占 2 B，剩余 504 B 采用直接索引结构，一个索引项占 6 B，则可支持的单个文件的最大长度是多少字节？为使单个文件的长度达到最大，请指出起始块号和块数分别所占字节数的合理值并说明理由。

> [!note]- 原题截图
> ![[王道OS 4.2 目录与文件 综合 13.webp]]

> [!success]- 答案与解析
> **答案：**
>
> 1. 4TB/1KB=2^32块，块号至少4B；512B索引区可放128项，最大文件128KB。
> 2. 题给6B起始块号、2B块数时，连续区最多2^16块=2^26B，剩504B可放84个6B直接项，最大为 `2^26B+84KB`。若追求理论最大，应把起始块号和块数均设4B：前者覆盖整个4TB空间，后者最多表示2^32块，使连续区可达4TB。
>
> **解析：** 直接索引上限由索引项数量决定；预分配区上限由块数字段宽度决定，同时起始块号必须覆盖全盘。

## 关联

- 知识点：
- 题型：
