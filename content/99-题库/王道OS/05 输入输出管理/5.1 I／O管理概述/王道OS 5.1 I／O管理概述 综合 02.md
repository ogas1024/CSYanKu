---
source: 王道OS
subject: OS
chapter: 05 输入输出管理
section: 5.1 I／O管理概述
qtype: 综合
topic: []
answer_status: 已录入
tags: []
---
## 题干

考虑 $56\,\mathrm{kb/s}$ 调制解调器的性能，驱动程序输出一个字符后就阻塞，当一个字符打印完毕后，产生一个中断通知阻塞的驱动程序，输出下一个字符，然后阻塞。

若发消息、输出一个字符和阻塞的时间总和为 $0.1\,\mathrm{ms}$，则由于处理调制解调器而占用的 CPU 时间比率是多少？假设每个字符有一个开始位和一个结束位，共占 10 位。

> [!note]- 原题截图
> ![[王道OS 5.1 I／O管理概述 综合 02.webp]]

> [!success]- 答案与解析
> **答案：**
>
> 每字符含10位，故每秒传输字符数为
> $$56000/10=5600.$$
> 每字符产生一次中断，每次中断相关CPU时间为 $0.1\,\mathrm{ms}$，所以每秒占用CPU时间
> $$5600\times0.1\,\mathrm{ms}=560\,\mathrm{ms},$$
> 占CPU时间的 $560/1000=56\%$。
>
> **解析：** 先把56 kb/s按每字符10位换算为中断频率，再乘每次发送、输出及阻塞的总CPU时间。

## 关联

- 知识点：
- 题型：
