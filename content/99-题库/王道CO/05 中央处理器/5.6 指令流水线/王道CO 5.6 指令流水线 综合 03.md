---
source: 王道CO
subject: CO
chapter: 05 中央处理器
section: 5.6 指令流水线
qtype: 综合
topic: []
answer_status: 已录入
tags: []
---
## 题干

【答案 P266】3. 【2014 统考真题】假设对于上题中的计算机 M 和程序 P 的机器代码，M 采用页式虚拟存储管理；

P 开始执行时， $(R1)=(R2)=0$， $(R6)=1000$，其机器代码已调入主存但不在 Cache 中；

数组 A 未调入主存，且所有数组元素在同一页，并存储在磁盘的同一个扇区。

请回答下列问题并说明理由。

- **（1）** P 执行结束时，R2 的内容是多少？

- **（2）** M 的指令 Cache 和数据 Cache 分离。

若指令 Cache 共有 16 行，Cache 和主存交换的块大小为 32B，则其数据区的容量是多少？若仅考虑程序段 P 的执行，则指令 Cache 的命中率为多少？

- **（3）** P 在执行过程中, 哪条指令的执行可能发生溢出异常? 哪条指令的执行可能产生缺页异常? 对于数组 A 的访问, 需要读磁盘和 TLB 至少各多少次?

> [!note]- 原题截图
> ![[王道CO 5.6 指令流水线 综合 03.png]]

> [!success]- 答案与解析
> **答案：**
>
> **（1）** 循环条件是 i<N，R2 从 0 自增到不再满足 i<1000，故结束时 **R2=1000**。
>
> **（2）** 指令 Cache 数据区容量为 $16\times32B=512B$。P 有 6 条指令共 24B，起始地址对应一个 32B 块的开头，所以全部位于同一主存块。1000 次循环共取指 6000 次，仅第一次缺失，命中率为
>
> $$(6000-1)/6000\approx99.98\%.$$
>
> **（3）** 第 **4** 条 add（sum+=A[i]）可能发生有符号溢出；第 **3** 条 load 可能发生缺页。A 全部在同一页且同一磁盘扇区，第一次访问调入后其余均命中，所以至少读磁盘 **1 次**。每次数组访问查 1 次 TLB，共 1000 次；首次 A[0] 先查 TLB 后缺页，处理后重启该指令又查一次，因此至少查 TLB **1001 次**。
>
> **解析：** 循环变量最终取边界值 N。指令块只需首次装入一次。数组页第一次访问造成一次可恢复缺页并重启 load，因此磁盘只读一次，但首次地址翻译会重复一次 TLB 查询。

## 关联

- 知识点：
- 题型：
